datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model idRoom {
  idRoom Int @unique
  users Json @default("[]")
  id String @id @default(auto()) @map("_id") @db.ObjectId
}